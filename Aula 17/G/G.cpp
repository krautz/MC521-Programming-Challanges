#include <cstdio>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <stdlib.h>
#include <string>
#include <vector>
#include <deque>
#include <map>
#include <queue>
#include <iomanip>

using namespace std;

typedef long long int lli;
typedef vector <lli> vetor;
typedef vector <vector <lli> > matriz;

struct lista {
  lli valor;
  lista *next;
};

int main () {
  lli numeros, entrada, operacao, origem, destino;
  lista *cabeca;

  cabeca = malloc (sizeof (lista));
  cabeca -> next = null;

  cin >> numeros

  return 0;
}
